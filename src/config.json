{
    "_description": "Application / Cloud Function default config.",
    "name": "myApp",
    "version": "0.0.0",
    "env": "development",
    "port": 8000,
    "logLevel": "info",

    "dev": {
        "_description": "settings if the environment is not 'production'.",
        "port": 8000,
        "logLevel": "error"
    }
}
